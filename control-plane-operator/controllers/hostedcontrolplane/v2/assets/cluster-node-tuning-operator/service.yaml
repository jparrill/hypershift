apiVersion: v1
kind: Service
metadata:
  labels:
    name: node-tuning-operator
  name: node-tuning-operator
spec:
  ports:
  - name: metrics
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    name: cluster-node-tuning-operator
  type: ClusterIP
